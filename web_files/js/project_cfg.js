/* 
项目 配置数据结构

此处为临时写死 , 实际应为从服务端获取
*/

var pjCfg={ //项目 位移, 角度数据 , 缩放数据 (应该加载项目时获取, 此处暂时写个临时数据)
    cfg:[//角度 ,位移 ,缩放信息
        300,200,// 0,1: translate( 300px,200px) 位移坐标
        0,0,0,  // 2,3,4:x,y,z  rotateX(0deg) rotateY(0deg)  rotateZ(0deg) 三轴默认旋转值
        1,      // 5:缩放倍率 默认1倍  scale3d(1,1,1)
        800,800,// 6,7: w,h 
        40,     // 8: 网格尺寸 默认40
    ],

}; 